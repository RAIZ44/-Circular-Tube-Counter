# Artifacts

This directory is filled by `python -m tube_count.train` and
`python -m tube_count.evaluate`. Checkpoints, metrics JSON, and qualitative
overlays are committed after a verified run so the report can be inspected
without retraining.
