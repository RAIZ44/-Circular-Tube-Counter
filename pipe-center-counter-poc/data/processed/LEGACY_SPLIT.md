These manifests preserve the original v1 split and labels. Their image paths
were migrated to portable paths relative to each manifest; membership is unchanged.

This split has known source-name overlap between training and evaluation.
Use data/processed_v2 for future training. Keep these manifests to identify the
development data of runs/pipe_center_v1/best.pt.

Original reports in this directory are historical and may contain old absolute paths.
