These 303 saved predictions were made on an existing Roboflow export, not a
newly captured field benchmark. 76 image paths occur directly in the original
training manifest; further related variants may overlap.

The predictions are useful visual examples. Do not report their accuracy as
independent field accuracy. Use scripts/field_benchmark.py with newly captured,
manually reviewed images for that purpose.
